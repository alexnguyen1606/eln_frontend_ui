import React from 'react'
import { withRouter } from 'react-router-dom'
function RoundTestInfo(props) {
    let { id } = props.match.params;

    return (
        <div>

        </div>
    )
}

export default withRouter(RoundTestInfo);
